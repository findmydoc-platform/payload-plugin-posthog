{
  "name": "payload-plugin-posthog",
  "version": "1.0.0",
  "description": "Plugin integrating PostHog into Payload CMS",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch", 
    "clean": "rm -rf dist",
    "prepublishOnly": "npm run clean && npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "payload",
    "plugin",
    "posthog",
    "analytics",
    "cms"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "posthog-node": "^4.0.1"
  },
  "peerDependencies": {
    "payload": "^2.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "payload": "^2.0.0",
    "typescript": "^5.0.0"
  },
  "files": [
    "dist"
  ]
}